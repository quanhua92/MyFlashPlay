import { describe, it, expect } from 'vitest';

describe('Markdown Parser', () => {
  it('should be tested when parser is available', () => {
    // Placeholder test until parseMarkdownToCards is exported
    expect(true).toBe(true);
  });

  // Additional tests would go here when parser is available
});